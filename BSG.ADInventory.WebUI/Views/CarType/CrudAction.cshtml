@model BSG.ADInventory.Models.CarType.CarTypeCEDTO
@using BSG.ADInventory.Resources
@{ViewBag.EntityName = "CarType";}

@using (Ajax.BeginForm("SetCrudAction", "CarType", new AjaxOptions { HttpMethod = "POST", UpdateTargetId = ViewBag.EntityName }))

{
    <div id="@ViewBag.EntityName">    
        <div class="form-horizontal">
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            @Html.HiddenFor(model => model.Id)
            @Html.Partial("CreateOrEdit", Model)
        </div>
    </div>

}
